# 🛡️ DSGVO Scanner - Technical Documentation

## 📋 Обзор продукта
**DSGVO Scanner** — это SaaS-платформа для автоматизированного аудита веб-сайтов на соответствие европейскому регламенту по защите данных (GDPR/DSGVO). Система имитирует действия реального пользователя с помощью headless-браузера для обнаружения скрытых нарушений.

---

## 🏗️ Технологический Стек
- **Frontend:** Next.js 15+ (App Router), React 19, TypeScript.
- **Styling:** Tailwind CSS 4, Shadcn/UI (премиальный дизайн, темная тема).
- **Backend/DB:** Supabase (Auth, PostgreSQL, Row Level Security).
- **Scanner Engine:** Playwright (Chromium) для глубокого динамического анализа.
- **Reporting:** `@react-pdf/renderer` для генерации White-Label PDF-отчетов.
- **Infrastructure:** Vercel (Deployment), GitHub Actions (CI/CD).

---

## 📂 Структура проекта (File Map)

### 1. Ядро Системы (`/lib`)
- `lib/scan-engine.ts` — **Сердце продукта**. Содержит логику сканирования, перехват сетевых запросов (Network Interception), анализ Cookies и расчет Risk Score.
- `lib/supabase/` — Конфигурация клиента Supabase (браузерная и серверная версии).
- `lib/hooks/` - Кастомные хуки для всей бизнес-логики (useDashboard, useScanner, и т.д.).
- `app/api/` - Эндпоинты для сканирования и отправки почты.
- `components/reporting/` - Генерация PDF и Email-модалки.
- `components/dashboard/` - Визуализация данных (Recharts).

### 2. Компоненты (`/components`)
- `components/ui/` — Переиспользуемые элементы интерфейса (Shadcn).
- `components/reporting/` — Модули для формирования отчетов.
  - `ScanReportPDF.tsx` — Шаблон PDF-документа.
  - `DownloadReportButton.tsx` — Логика подготовки данных и скачивания.

### 3. Страницы (`/app`)
- `app/dashboard/` — Личный кабинет пользователя.
- `app/dashboard/websites/` — Управление списком сайтов клиентов.
- `app/dashboard/settings/` — Настройки профиля и **White-Label** (брендинг).
- `app/impressum/` & `app/datenschutz/` — Юридические страницы (Spain-Safe Strategy).

### 4. Типы и База Данных (`/types`, `/supabase`)
- `types/supabase.ts` — TypeScript интерфейсы, синхронизированные со схемой БД.
- `supabase/schema.sql` — Полная структура таблиц, RLS-политики и триггеры.
- `supabase/phase3_agencies.sql` — Описание таблицы для агентского брендинга.

---

## 🏗️ Архитектура и Интеграции

### Email & Reporting
- **Resend Integration**: Используется для отправки ссылок на интерактивные отчеты клиентам. Шаблоны писем находятся в `app/api/send-report/route.ts`.
- **PDF Generation**: `@react-pdf/renderer` используется для генерации брендированных отчетов с использованием шрифта Inter для корректного отображения немецких спецсимволов.

### Scanner Resilience
- **Wait Condition**: Переключено с `networkidle` на `load` для ускорения и избежания таймаутов на тяжелых сайтах.
- **Auto-Scroll**: Ускоренная прокрутка (400px/50ms) с ограничением по высоте (15k px) для захвата лениво-загружаемых ресурсов.
- **Fail-safe**: При возникновении таймаута сканер пытается проанализировать уже загруженный HTML вместо прерывания процесса.

### Dashboard Analytics
- **Recharts**: Интегрирован `AreaChart` для визуализации "Risk Trend" за последние 7 дней. Данные агрегируются в хуке `useDashboard`.

### SEO Stack
- **Robots/Sitemap**: Динамическая генерация через `app/robots.ts` и `app/sitemap.ts`.
- **Metadata**: Расширенные теги в `layout.tsx` и JSON-LD микроразметка в `app/page.tsx`.

## ⚙️ Как работает Сканер (Deep Dive)

### Процесс анализа (`analyzeWebsite`):
1. **Инициализация:** Запуск браузера Chromium через Playwright.
2. **Перехват запросов (Interception):** 
   - Слушатель `page.on('request')` проверяет каждый URL на соответствие паттернам (Google Fonts, GTM, GA и др.).
   - Это позволяет находить ресурсы, которые подгружаются динамически.
3. **Эмуляция пользователя:** 
   - **Auto-scroll:** Скрипт прокручивает страницу до конца, чтобы сработали Lazy Loading шрифты и скрипты.
   - **Wait:** Ожидание `networkidle`, когда сетевая активность утихает.
4. **Анализ Cookies:** Проверка всех куки, установленных *до* того, как пользователь мог нажать "Принять" на баннере.
5. **Scoring:** Расчет оценки безопасности от 0 до 100 на основе важности найденных нарушений.

---

## 💼 White-Label & Multi-Tenancy
Система спроектирована для агентств:
- **Таблица `agencies`:** Хранит настройки бренда (логотип, Hex-код цвета, футер).
- **Supabase Storage:** Бакет `agency-logos` для хранения изображений брендинга с RLS по `owner_id`.
- **RLS (Row Level Security):** Гарантирует, что данные сайтов и настройки агентства доступны только их владельцу (`owner_id = auth.uid()`).
- **PDF-генерация:** Происходит на стороне клиента с использованием шрифта Inter (UTF-8) для корректного отображения немецких текстов.

---

## 🛠 Разработка и Тестирование
- **Локальный запуск:** `npm run dev` и `npx supabase start`.
    - *Примечание для Windows:* Если возникает ошибка 502, отключите `analytics` в `supabase/config.toml`.
- **Тесты (Vitest):** `npm test`. Модульные тесты находятся в `__tests__/unit/`. Они используют моки для Playwright, чтобы проверять логику анализатора без реального запуска браузера.
- **Сборка:** `npm run build` (используется для проверки перед деплоем на Vercel).

---

## 🚀 Будущее (Roadmap)
1. **AI Impressum Validator:** Использование LLM для проверки наличия и корректности юридических данных на сайте.
2. **Paddle Integration:** Автоматический биллинг и управление подписками.
3. **Advanced Cookies Analysis:** Классификация куки по категориям (Marketing, Functional, Necessary).

---
*Документация актуальна на 01.02.2026. Разработчик: Antigravity.*
