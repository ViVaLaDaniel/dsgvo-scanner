# 🤖 AI Development Workflow Manual

Это инструкция к вашей "AI-Команде" (Jules + Gemini).

> **Важное правило:** любые инициативы и исследования должны соотноситься с [`docs/ROADMAP.md`](../ROADMAP.md).

## 🚀 1. Ежедневный запуск (Jules)
Чтобы синхронизировать код и запустить аудитора Jules на поиск ошибок:

```powershell
./scripts/start_work.ps1
```
*Запускать каждое утро перед работой.*

---

## 🧠 2. Управление проектом и Интеграция (Gemini CLI)
Gemini CLI теперь выступает в роли главного оркестратора и интегратора, управляя ветками, документацией и настройкой инфраструктуры.

Чтобы попросить "Мозг" (Gemini) проанализировать рынок, технологии или конкурентов. Он использует контекст вашего проекта.

```powershell
scripts/research.ps1 "Ваш вопрос или тема"
```
**Примеры:**
* `"Как улучшить SEO для немецкого рынка?"`
* `"Сравни библиотеки для генерации PDF в Next.js"`
* `"Напиши маркетинговый план для запуска MVP"`

*Отчеты сохраняются в папку `.ai/research/`.*

---

## 🗺️ 3. Структура "Секретных папок"
Эти папки существуют только у вас (в Git они скрыты):

| Путь | Описание |
| :--- | :--- |
| **`.ai/CONTEXT.md`** | **Паспорт проекта.** Главный файл. Если меняется стек или цели — правьте его. |
| **`.ai/CURRENT_PLAN.md`** | **План работ.** Jules обновляет его, вы ставите галочки. |
| **`.ai/daily_reports/`** | Тут лежат отчеты Jules об ошибках в коде. |
| **`.ai/research/`** | Тут лежат аналитические отчеты от Gemini. |

---

## 🛠️ 4. Полезные команды
* `jules login` — если просит вход.
* `npm run dev` — запуск локального сервера.
* `npm test` — запуск тестов (требует Node.js 20+).
* `docker-compose up --build` — запуск проекта с Docker.

---

## 🆘 5. Решение проблем (Troubleshooting)

### Почта не отправляется?
1. Проверь наличие `RESEND_API_KEY` в `.env.local`.
2. Если используешь `onboarding@resend.dev`, письмо придет **только тебе**. Для отправки клиентам нужно подтвердить домен в панели Resend.
3. Проверь логи сервера в терминале при нажатии кнопки.

### Сканер выдает Timeout?
1. В `lib/scan-engine.ts` мы сменили ожидание на `waitUntil: 'load'`. Если сайт все еще падает, увеличь `timeout: 45000` до более высокого значения.
2. Для продакшн-версии (Vercel) используется удалённый микросервис через `SCANNER_MICROSERVICE_URL` и `SCANNER_SECRET`.

### Ошибка при копировании ссылки?
Мы внедрили fallback: если браузер блокирует `navigator.clipboard` (из-за отсутствия HTTPS), система создаст невидимое текстовое поле и скопирует ссылку через него.

### Ошибки в тестах (Vitest)?
На Windows и Node.js 21 могут возникать ошибки ESM. Рекомендуется использовать **Node.js 20 LTS** или **22+**. Конфиг тестов находится в `vitest.config.mts`.

### Проблемы с Docker?
1. Убедитесь, что Docker Desktop запущен.
2. Проверьте логи контейнеров через `docker-compose logs`.
3. Убедитесь, что порты `3000` и `3001` не заняты другими приложениями на вашей машине.