# Технический Аудит Проекта: DSGVO Scanner

**Дата:** 24.05.2024
**Статус:** Critical / Pre-Alpha
**Тип:** UI Prototype (Mock)

Этот документ представляет собой "brutally honest" технический аудит текущей кодовой базы. Цель аудита — передать реальное состояние проекта Системному Архитектору для планирования разработки с нуля.

---

## 1. PROJECT STRUCTURE & STACK

### Зависимости (package.json)
Проект использует современный Frontend-стек, однако полностью отсутствуют инструменты для реализации заявленного Backend-функционала (сканирование сайтов).

*   **Frontend:** `next` (16.1.3), `react` (19.2.3), `tailwindcss` (4), `lucide-react`.
*   **Backend / DB:** `@supabase/supabase-js`, `@supabase/ssr`.
*   **MISSING (Критически отсутствующие зависимости):**
    *   ❌ `puppeteer` / `playwright` (нет headless-браузера для рендеринга сайтов).
    *   ❌ `cheerio` / `jsdom` (нет парсеров HTML).
    *   ❌ `axios` / `node-fetch` (нет HTTP-клиентов для серверных запросов).
    *   ❌ `pdfkit` / `react-pdf` (нет генераторов PDF).

### Структура папок
*   📂 `app/` — Next.js App Router.
*   ❌ `app/api/` — **ОТСУТСТВУЕТ**. В проекте нет ни одного API Route Handler. Серверная логика полностью отсутствует.
*   📂 `components/` — UI компоненты (Shadcn/UI).
*   📂 `lib/supabase/` — Клиентская инициализация Supabase.

---

## 2. SCANNING LOGIC (Crucial)

**Статус:** 🔴 **NON-EXISTENT (Hardcoded Mock)**

В проекте **нет** реального движка сканирования. Вся логика "анализа" является фикцией (Mock), реализованной на стороне клиента (Client-Side) внутри React-компонента.

**Файл:** `app/dashboard/scans/[id]/page.tsx`

### Как это работает сейчас:
Код проверяет наличие подстрок `vitbikes.de` или `google` в URL. Если они есть — добавляются заранее заготовленные статические данные о нарушениях. Никаких запросов к проверяемому сайту не выполняется.

### Текущий код (Evidence):
```typescript
// app/dashboard/scans/[id]/page.tsx

async function performScan() {
  // ...
  const url = currentWebsite.url.toLowerCase();
  const detectedFindings = [];
  let score = 100;

  // ⚠️ HARDCODED MOCK LOGIC
  // Реального сканирования нет. Просто проверка строки URL.
  if (url.includes('vitbikes.de') || url.includes('google')) {
     detectedFindings.push({
        category: 'Drittanbieter & CDNs',
        title: 'Google Fonts (Remote)',
        description: 'Schriftarten werden direkt von Google-Servern...',
        // ... статический текст
     });
     score -= 15;
  }

  // ... (аналогичные if-блоки для GTM и YouTube)

  // Имитация задержки загрузки для "реалистичности"
  // Но по факту - это просто if/else заглушка.
}
```

**Вывод:** Текущая реализация не подлежит доработке. Требуется полная разработка микросервиса сканирования с нуля.

---

## 3. DATABASE & AUTH

**Статус:** 🟠 **Technical Debt / No Migrations**

*   **Подключение:** Supabase подключен (`lib/supabase/client.ts`). Используется клиентская библиотека (`createClient`) внутри `useEffect`.
*   **Схема БД:** ❌ **ОТСУТСТВУЕТ в репозитории**.
    *   Нет папки `supabase/migrations`.
    *   Нет `.sql` файлов.
    *   История изменений схемы базы данных не версионируется. Если БД будет удалена, восстановить структуру проекта из кода невозможно.
*   **Безопасность:** Вся логика выборки данных находится на клиенте. Безопасность полностью зависит от настроек RLS (Row Level Security) в самой Supabase, которые не видны в коде.

---

## 4. SEO & METADATA CHECK

**Статус:** 🔴 **Critical UI/UX Inconsistency**

Продукт заявлен как немецкий B2B инструмент, но метаданные и настройки языка не соответствуют требованиям.

**Файл:** `app/layout.tsx`

```tsx
export const metadata: Metadata = {
  title: "Create Next App",       // ❌ Дефолтный заголовок Next.js
  description: "Generated by create next app", // ❌ Дефолтное описание
};

export default function RootLayout({ children }: ...) {
  return (
    <html lang="en">             {/* ❌ Язык установлен как Английский */}
      {/* ... */}
    </html>
  );
}
```

**Проблема:**
*   Смешанная локализация: Интерфейс на немецком, disclaimer в отчете на русском ("Этот отчет был создан автоматически..."), метаданные на английском.
*   Это недопустимо для Production-ready продукта.

---

## 5. MISSING CRITICAL FILES

**Статус:** 🔴 **Missing Features**

1.  **PDF Generation:** ❌ **Отсутствует**.
    *   Кнопка "PDF Export" вызывает `window.print()`.
    *   **Почему это плохо:** Клиенты (DSB/Агентства) ожидают физический PDF файл, сгенерированный на сервере, который можно прикрепить к Email. Распечатка HTML-страницы браузером выглядит непрофессионально (обрезаются стили, виден UI браузера).

2.  **Legal Pages:**
    *   ❌ `app/impressum/page.tsx` — Не найден.
    *   ❌ `app/datenschutz/page.tsx` — Не найден.
    *   Для немецкого рынка отсутствие Impressum является юридическим нарушением (Abmahngefahr).

---

## ИТОГОВОЕ ЗАКЛЮЧЕНИЕ

Текущий репозиторий представляет собой **UI-прототип (MVP Click-dummy)**, а не функционирующий SaaS-продукт.

*   **Готовность Backend:** 0% (Сканер отсутствует).
*   **Готовность Frontend:** 65% (UI есть, Context API внедрен для оптимизации, но требует чистки от хардкода и локализации).
*   **Готовность Инфраструктуры:** 20% (Supabase подключен, но нет миграций).

### Recent Improvements (Performance)
*   **Context API Integration:** Внедрен `DashboardContext` для устранения проблемы "Double Fetching" (сетевой водопад) на главной странице дашборда. Данные профиля и счетчики загружаются один раз в Layout.

**Рекомендация Архитектору:** Использовать текущий UI (компоненты Shadcn) как основу для дизайна, но архитектуру приложения строить с нуля, внедрив Node.js/Python микросервис для реального сканирования (Puppeteer) и серверную генерацию PDF.
